// // sendPushNotification();
// // sendSmsNotification(task[index]);
// // sendEmailNotification(task[index]);
// import { scheduleJob } from "node-schedule";

// export const sendPushNotification = (task) => {
//   console.log(" task sendPushNotification ", task);
// };
